List:
? for my $item(@{ $_[0]->{data} }) {
    * <?= $item->{title} ?>
? }

List:
? for my $item(@{ $_[0]->{data} }) {
    * <?= $item->{title} ?>
? }

List:
? for my $item(@{ $_[0]->{data} }) {
    * <?= $item->{title} ?>
? }
